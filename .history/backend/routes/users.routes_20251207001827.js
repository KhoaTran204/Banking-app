const express = require("express");
const router = express.Router();
const controller = require("../controller/controller");
const usersScheme = require("../model/controller");

router.post("/", (req, res) => {});

module.exports = router;
